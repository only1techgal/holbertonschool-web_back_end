/**
 * Converts an array into a Set
 * @param {Array} array - The array7 to be converted into a set
 * returns {Set} A set constaining the unique values from the array
 */
export default function setFromArray(array) {
    return new Set (array);
}